import JobOpening from "../(Components)/JobOpening";
import OpenPosition from "../(Components)/OpenPosition";

const page = () => {
  return (
    <main>
      <JobOpening />
      <OpenPosition />
    </main>
  );
};

export default page;
